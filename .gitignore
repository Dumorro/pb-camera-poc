node_modules/
dist/
# src/lib/wasm-pkg/ — committed so Vercel doesn't need Rust toolchain
wasm/target/
wasm/pkg/
*.local
.DS_Store
*.tsbuildinfo
*.log
.vercel
# OpenCV.js — download separately, too large for git (~8MB)
public/opencv/
